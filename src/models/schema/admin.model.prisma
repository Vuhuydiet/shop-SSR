model Admin {
  adminId  Int       @id @default(autoincrement())
  fullname String
  doB      DateTime? @db.Date
  email    String?   @unique
  gender   String?

  username       String
  hashedPassword String
}
