model Product {
  id           Int     @id @default(autoincrement())
  name         String
  description  String
  initialPrice Float
  price        Float
  stock        Int
  image        String?
  category     String
  brand        String
  stars        Float
  numReviews   Int
}
