model User {
  id            Int      @id @default(autoincrement())
  username      String   @unique
  password      String
  lastLogin     DateTime
  createdAt     DateTime @default(now())
  email         String? 
  phoneNumber   String?
}